id: read-file-absolute-param-satisfied
language: Rust
severity: info
message: target_file schemars already requires absolute path only (Claude Code FileReadTool).
rule:
  kind: string_literal
  regex: The absolute path to the file to read
